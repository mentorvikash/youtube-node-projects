<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Weather App</title>
  </head>
  <body>
    <h1>Weather App</h1>
    <form method="post" action="/weather">
      <label for="cityName">Enter Your City</label>
      <input type="text" id="cityName" name="cityName" required />
      <button type="submit">Get Data</button>
    </form>
    <% if (weatherData) { %>
    <div>
      <h2>Weather Details of <%= weatherData.name %></h2>
      <h3>Temperature: <%= weatherData.main.temp %></h3>
      <h3>Min Temp: <%= weatherData.main.temp_min %></h3>
      <h3>Max Temp: <%= weatherData.main.temp_max %></h3>
      <h3>Pressure: <%= weatherData.main.pressure %></h3>
      <h3>Humidity: <%= weatherData.main.humidity %></h3>
      <h3>Wind Speed: <%= weatherData.wind.speed %></h3>
    </div>
    <% } else { %>
    <div><%= error %></div>
    <% } %>
  </body>
</html>
